<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qiein.erp.pk.web.dao.VenueScheduleDao" >
        <select id="getVenueSchedule" resultType="com.qiein.erp.pk.web.entity.vo.OrderVenueScheduleVO">
            SELECT
                sd.venue_id venueId,
                sd.venue_day venueDay,
                sd.venue_day_limit venueDayLimit,
                sd.service_id serviceId,
                count(1) count
            FROM
                hm_erp_order_venue_schedule ord
            LEFT JOIN hm_erp_venue_schedule sd ON sd.id = ord.venue_schedule_id
            AND sd.company_id = ord.company_id
            where sd.venue_day BETWEEN  #{start} and #{end}
            group by ord.venue_schedule_id
        </select>

   <!-- <resultMap id="BaseResultMap" type="com.qiein.erp.pk.web.entity.po.ShootSchedule" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="id" property="baseId" jdbcType="INTEGER" />
        <result column="shoot_id" property="venueName" jdbcType="VARCHAR" />
        <result column="scene_id" property="venueType" jdbcType="SMALLINT" />
        <result column="venue_phone" property="venuePhone" jdbcType="VARCHAR" />
        <result column="respo" property="respo" jdbcType="VARCHAR" />
        <result column="address" property="address" jdbcType="VARCHAR" />
        <result column="venue_status" property="venueStatus" jdbcType="SMALLINT" />
        <result column="company_id" property="companyId" jdbcType="INTEGER" />
        <result column="priority" property = "priority" jdbcType="INTEGER"/>
    </resultMap>


    <select id="selectScheduleByDateTime" parameterType="map">

        select id,shoot_id,scene_id,start_time，end_time，status_id
        from hm_erp_shoot_schedule



    </select>-->

</mapper>